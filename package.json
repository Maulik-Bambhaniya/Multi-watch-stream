{
  "name": "multistream",
  "version": "1.0.0",
  "description": "Multi-platform live stream viewer - Watch multiple streams together",
  "scripts": {
    "dev": "concurrently -n \"frontend,backend\" -c \"cyan,green\" \"npm run dev --prefix frontend\" \"cd backend && go run ./cmd/server/main.go\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "cd backend && go run ./cmd/server/main.go",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  },
  "keywords": [
    "multistream",
    "twitch",
    "youtube",
    "kick",
    "streaming"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
